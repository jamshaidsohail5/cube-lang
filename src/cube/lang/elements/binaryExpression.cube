use cube.lang

define binaryExpression as element
  function create(operator as operator, left as element, right as element)
    me.operator = operator
    me.left = left
    me.right = right
  end

  internal
    var operator as operator
    var left as element
    var right as element
  end
end