use cube.lang

define functionExpression as element
  function create(target as element, arguments as argumentList)
    me.target = target
    me.arguments = arguments
  end

  internal
    var target as element
    var arguments as argumentList
  end
end