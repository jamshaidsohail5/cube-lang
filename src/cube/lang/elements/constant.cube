use cube.lang

define constant as element
  function create(value as object) = me.value = value
  function write(s as stringBuilder) as void = s.append(value)

  internal
    var value as object
  end
end