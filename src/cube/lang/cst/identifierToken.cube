define identifierToken as token
  function create(text as string) = super(NodeType.Identifier, text)
  function key as object = NodeType.Identifier

  protected
    function write(s as stringBuilder)
      s.append(nodeType.displayName)
      s.append("('")
      s.append(text)
      s.append("')")
    end
  end
end