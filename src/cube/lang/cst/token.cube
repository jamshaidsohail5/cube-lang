define abstract token as cstNode
  function create(nodeType as cstNodeType, text as string)
    super(nodeType)
    me.text = text
  end

  function text as string = text
  function toString as string = text

  abstract
    function key as object
  end

  protected
    var text as string
  end
end